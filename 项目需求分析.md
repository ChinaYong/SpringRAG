# 项目需求分析

> 此项目根本上用于本人学习所创建，需要注意不要偏移这一目的。由于这比较极端的目的，程序设计上可能出现一些不合理的地方，当然，知道这些不合理的地方并解决也是学习的一部分。

## 一、项目大纲

这个项目是一个常见的问答前后端分离系统，以 LLM 为底层服务，为用户提供问答功能。它包括但不限于以下功能：

1. 基础对话功能：用 LLM 回复用户问题
   - 支持模型切换
   - 支持 Markdown 渲染
   - 支持修改模型参数
   - 支持流式输出
   - 支持多轮对话
   - 支持用户上传文件，基于文件内容生成对话
2. 历史对话管理：用户可以查看自己的历史对话内容。
3. 内置模型提供商且支持自定义提供商：需自行提供API
4. 支持自定义智能体：每个智能体拥有独立的 System Prompt、历史对话、模型参数设置等。
5. 账号系统
   - 注册
   - 登录
   - 用户权限
6. 系统安全性与非功能需求：
   1. 用户与权限管理
      - 引入 JWT 认证机制，隔离用户间的必要数据。
   2. API 密钥安全
      - LLM 提供商的 API 密钥必须使用加密存储。
   3. 接口防刷限流
      - 针对对话和文件上传接口，要实现必要的防护措施。
   4. 异步任务状态跟踪
      - 文件的解析与向量化必须异步进行，且需要向前端暴露解析状态（待解析、解析中、已完成、失败）

## 二、技术栈

1. 前端：Vue 3
2. 后端：

   - 基础环境： Java 25
   - 核心框架： SpringBoot 3.5.14
   - 数据库操作（ORM）： MyBatis-Plus
   - 数据库：
     - 关系型数据库： MySQL（存储用户信息、智能体配置、对话历史等结构化数据）
     - 向量数据库：Qdrant（存储文档切片等向量数据）
     - NoSQL 数据库：Redis
   - AI 核心： Spring AI
   - 权限与安全： JWT + Spring MVC 拦截器
   - 缓存与限流： Spring Data Redis
   - 文档解析提取：Spring AI 原生的 DocumentReader
3. 工具：

   - 版本管理：Git + GitHub
   - 依赖管理：Maven

## 三、数据库设计

### 1、关系型数据库（MySQL）

#### 1）需求

1. 存储用户信息。
2. 存储智能体信息。
3. 存储用户与智能体的会话管理信息。
4. 存储对话内容。
5. 存储文件信息。
6. 存储模型提供商信息。

#### 2）数据库表结构

1. 用户表（user）：存储用户信息。

   | 字段名      | 字段类型     | 约束       | 注释                       |
   | ----------- | ------------ | ---------- | -------------------------- |
   | id          | INT          | 主键、自增 | 用户ID                     |
   | username    | VARCHAR(20)  | 非空、唯一 | 用户登录时的账号           |
   | password    | VARCHAR(255) | 非空       | 并非密码，而是密码的哈希值 |
   | create_time | DATETIME     |            | 创建时间                   |
   | update_time | DATETIME     |            | 更新时间                   |

2. 智能体表（agent）：存储智能体信息。

   | 字段名           | 字段类型    | 约束       | 注释                                                             |
   | ---------------- | ----------- | ---------- | ---------------------------------------------------------------- |
   | id               | INT         | 主键、自增 | 智能体ID                                                         |
   | user_id          | INT         | 非空       | 智能体所属用户ID                                                 |
   | name             | VARCHAR(20) | 非空       | 智能体名称                                                       |
   | description      | TEXT        |            | 智能体描述                                                       |
   | system_prompt    | TEXT        |            | 智能体系统提示词                                                 |
   | model_id         | INT         |            | 智能体默认使用的模型ID，可以为空，但必须在与智能体对话时指定模型 |
   | model_parameters | JSON        |            | LLM的模型参数                                                    |
   | file_ids         | JSON        |            | 智能体绑定的文件，参照 file_storage 中的ID                       |
   | create_time      | DATETIME    |            | 创建时间                                                         |
   | update_time      | DATETIME    |            | 更新时间                                                         |

3. 会话管理表（session）：存储用户与智能体的会话管理信息。

   | 字段名      | 字段类型    | 约束       | 注释             |
   | ----------- | ----------- | ---------- | ---------------- |
   | id          | INT         | 主键、自增 | 会话ID           |
   | user_id     | INT         | 非空       | 会话所属用户ID   |
   | agent_id    | INT         | 非空       | 会话所属智能体ID |
   | name        | VARCHAR(20) | 非空       | 会话名称         |
   | create_time | DATETIME    |            | 创建时间         |
   | update_time | DATETIME    |            | 更新时间         |

4. 对话内容表（chat_message）：存储对话内容。

   | 字段名      | 字段类型                  | 约束                                | 注释                         |
   | ----------- | ------------------------- | ----------------------------------- | ---------------------------- |
   | session_id  | INT                       | 非空、外键参照session表id、联合主键 | 会话ID                       |
   | role        | ENUM('user', 'assistant') | 非空                                | 消息角色                     |
   | content     | TEXT                      | 非空                                | 对话内容                     |
   | tokens      | INT                       |                                     | Token数量                    |
   | order       | INT                       | 唯一、非空、联合主键                | 对话的序号                   |
   | file_ids    | JSON                      |                                     | 用户上传的附件（文件ID列表） |
   | create_time | DATETIME                  |                                     | 创建时间                     |
   | update_time | DATETIME                  |                                     | 更新时间                     |

5. 用户文件信息表（user_file_info）：存储用户的文件信息。

   | 字段名      | 字段类型     | 约束       | 注释             |
   | ----------- | ------------ | ---------- | ---------------- |
   | id          | INT          | 主键、自增 | 文件ID           |
   | user_id     | INT          | 非空       | 文件关联的用户ID |
   | name        | VARCHAR(255) | 非空       | 文件原始名称     |
   | file_id     | INT          | 非空       | 文件存储表中的ID |
   | create_time | DATETIME     |            | 创建时间         |
   | update_time | DATETIME     |            | 更新时间         |

6. 文件存储表（file_storage）：存储文件内容。

   | 字段名            | 字段类型     | 约束 | 注释         |
   | ----------------- | ------------ | ---- | ------------ |
   | id                | INT          | 主键 | 文件ID       |
   | sha256            | VARCHAR(64)  | 唯一 | 文件存储ID   |
   | storage_path      | VARCHAR(255) |      | 文件存储路径 |
   | size              | BIGINT       |      | 文件大小     |
   | extension         | VARCHAR(10)  |      | 文件扩展名   |
   | ref_count         | INT          |      | 文件引用计数 |
   | vectorized_status | TINYINT      |      | 向量化状态   |
   | create_time       | DATETIME     |      | 创建时间     |
   | update_time       | DATETIME     |      | 更新时间     |

7. 模型提供商表（model_provider）：存储模型提供商信息。

   | 字段名       | 字段类型     | 约束       | 注释                 |
   | ------------ | ------------ | ---------- | -------------------- |
   | id           | INT          | 主键、自增 | 模型提供商ID         |
   | user_id      | INT          | 非空       | 模型提供商所属用户ID |
   | name         | VARCHAR(20)  | 非空       | 模型提供商名称       |
   | api_key      | VARCHAR(255) | 非空       | 模型提供商API密钥    |
   | api_base_url | VARCHAR(255) | 非空       | 模型提供商API基础URL |
   | model_list   | JSON         | 非空       | 模型列表             |
   | create_time  | DATETIME     |            | 创建时间             |
   | update_time  | DATETIME     |            | 更新时间             |

### 2、向量数据库（Qdrant）

#### 1） 需求

存储以及检索向量数据。

#### 2） 设计

Point 结构：

1. ID ： UUID
2. Vector ： 向量数据
3. Payload ： Qdrant 中 Payload 是 Map 类型的键值对（JSON）。
   - file_id(Integer) : 文件ID，参照 MySQL 中的 file_info.id。
     - 关联向量与文件
   - file_path(String)：文件存储路径
   - user_id(Integer) : 上传该文件的用户 ID。
     - 关联向量与用户
   - doc_content(String) : 向量的原始文本内容。
     - 需要保存原始文本内容便于后续的 Prompt 拼接
     - Spring AI 自动填充
   - chunk_index(Integer) : 向量在原始文件中的序号。
     - 记录每个向量的原始顺序，可以通过相同的 parent_document_id 和前后的 chunk_index 找到某段的上下文
     - Spring AI 自动填充
   - file_name(String) : 文件名称。
     - 方便在检索结果中显示文件名
   - parent_document_id(Integer): 分块前的文档 ID
     - Spring AI 会自动生成文档 ID，后续 ID 会被作为 parent_document_id 保留
   - total_chunks(Integer): 总分块数
     - Spring AI 自动填充
   - source(String): 以“file”开头的文件路径
     - Spring AI 自动填充

### 3、NoSQL数据库（Redis）

#### 1） 需求

1. 缓存高频访问的热点数据（读多写少）
   - 智能体配置
   - 模型提供商配置
2. 接口防刷限流
   - 记录用户调用对话接口、上传文件接口的频率
3. 异步任务状态管理
   - 存储文件解析、向量化过程中的实时状态（待解析、解析中、已完成、失败）
4. JWT 黑名单
   - 存储用户登出、被封禁时的 JWT Token

#### 2）设计

1. SSE 连接维护
   1. 文件处理过程
      - key: "file:progress:{fileId}"
      - value:

      ```json
      {
         "stage": 处理的阶段
         "status": 当前的文件处理状态,
         "progress": 文件处理的百分比进度,
         "message": 相关消息
      } 
      ```

   2. 订阅文件处理过程的任务
      - key: "file:tasks:{fileId}"
      - value:
         task 集合，记录追踪对应文件 id 文件的处理进度

   3. 用户权限校验
      - key: upload:task:{taskId}
      - value:

      ```json
      {
         "userId": 用户ID,
         "fileId": 文件ID,
         "status": 文件处理状态,
         "stage": 文件处理阶段
      }
      ```

### 4、功能分析与实现

#### 1） 用户登录与注册

#### 2） 历史对话管理

> 历史对话管理涉及到用户的历史的会话管理以及一次会话的历史记录，因此就需要解决两个问题：1. 会话如何管理？ 2. 对话内容如何管理？但又涉及到对 LLM 上下文的管理，这是重点。

##### 1. 会话管理

用户登录后，系统需要生成一个历史会话的列表，这一点只需要查询 session 表即可，后端只需要实现 session 表的 CRUD 即可。

##### 2. 对话内容管理

对于前端展示会话列表，用户点击某次会话，需要展示对话的历史记录，后端只需要实现 chat_message 表的 CRUD 。

##### 3. LLM 上下文管理

需要在向 LLM API 发送请求前，构建上下文，这里的上下文包括：对话历史、System Prompt、知识库之类的 RAG 检索到的内容。对每种上下文进行分析：

1. 对话历史： 用户发出对话请求前，一定会读取历史对话，所以前端可以直接在对后端发送请求前，构建好历史对话上下文后一并发送给后端，但是这就意味着前端给后端发送的消息体较大，占用更多的网络带宽，但是如果是后端构建历史对话的上下文，则需要后端从 session 表中读取历史对话，再构建对话历史，读取数据库内容同样需要占用网络带宽。还要多一次数据库查询。
2. System Prompt： 和对话历史的构建需要解决的问题基本相同。
3. 知识库 RAG 检索内容： 唯独这个肯定是在后端处理，因为需要根据请求的对话内容来生成向量，再检索向量数据库，查找一定程度符合的知识片段后，再进行 Prompt 的拼接。

#### 3） 文件上传、管理与向量化

##### 1. 文件上传与管理

文件上传有两种途径：1. 在智能体中作为知识库上传。 2. 在一次对话中作为上下文上传。在用户上传文件后，对于对话，即使用户未点击发送，也需要立刻对文件进行处理（向量化），在智能体中作为知识库上传的文件更应该如此。

文件的管理： 用户上传的文件，需要将文件存储到服务器上，同时在 MySQL 中留下数据，并在 Qdrant 中留下数据，这里可以考虑使用多线程加速。

##### 2. 文件的向量化

文件的向量化本质是将文件进行分块处理，对每个块进行向量化，目标是要让每个块包含文件的相对独立的信息，最终所有块的和应该能拼凑出文件的完整信息。因此，分块的策略就尤其重要。对于最基本的文本内容，保证信息相对独立的语义，最容易想到的策略就是按段落、按句子进行分割。对于字数长度适中的段落，直接按段落分块，如果按照段落分块的策略导致某一块长度过长，需要将该段落按句子分割，为了保证完整的语义，给予适当的冗余，即：相邻块之间保留一定程度的重叠。

#### 4） RAG 检索

需要根据用户的提问，对知识库以及用户上传的文件的内容进行检索。用户提问里包含的内容除了此次的发问，还包含历史的上下文，检索的时候如果不基于上下文进行检索，会让 RAG 提取的内容经常失去参考价值，并且容易给模型带来不必要的噪音。所以可以确定要基于对话历史上下文进行检索，另外，需要将 System Prompt 进行专门的 RAG 检索以提高知识库与 System Prompt 的联动，也可以将这个检索的结果直接持久化保存。

虽说已经确定了要基于 RAG 上下文进行检索，但是考虑到 LLM 的输入有限，有必要对上下文进行压缩。具体方法可以是：用新参数模型进行信息提取等。
